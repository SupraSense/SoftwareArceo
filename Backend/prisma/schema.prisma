datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model TipoTarea {
  id     Int    @id @default(autoincrement())
  nombre String @unique
}